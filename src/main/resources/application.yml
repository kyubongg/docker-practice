spring:
  application:
    name: docker-practice
  datasource:
    url: jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3307}/${MYSQL_DATABASE:docker-practice-db}?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: ${DB_USER:bong}
    password: ${DB_PASSWORD:bong123}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true